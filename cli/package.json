{
  "name": "@leaseguard/cli",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Wave 8 Part C — leaseguard-verify CLI (test scaffolding only).",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -b --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
